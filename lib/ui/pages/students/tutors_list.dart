import 'package:flutter/material.dart';

// import 'import/Signin.dart';

class Tutor extends StatelessWidget {
  @override
  Widget build(BuildContext context) {
    return Scaffold(
      body: new Text('Tutors List'),
    );
  }
}